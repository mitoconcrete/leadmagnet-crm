meta {
  name: links-list
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/admin/forms/{{formId}}/links
  body: none
  auth: none
}

headers {
  Cookie: sid={{sid}}
}

assert {
  res.status: eq 200
}
