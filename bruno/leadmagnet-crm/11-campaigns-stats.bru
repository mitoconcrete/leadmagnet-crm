meta {
  name: campaigns-stats
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/admin/campaigns/{{campaignId}}/stats
  body: none
  auth: none
}

headers {
  Cookie: sid={{sid}}
}

assert {
  res.status: eq 200
  res.body.campaignId: eq {{campaignId}}
}
